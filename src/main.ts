
import { batsman } from './batsman';
import { bowler } from './bowler';
import { Ball } from './ball';
var input:Array<Ball>;
input[0].runsScored=0;
input[0].isOut=true;
input[0].dismissalType='run out';

for(let i=0;i<input.length;i++)
{
    if(input[i].isOut)
        {
        
        }
}